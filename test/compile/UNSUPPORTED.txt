# GCC c-torture compile-only tests currently unsupported / non-OK in FakeCC
# Total: 5 (out of 2003 original GCC compile cases)
#
# Harvest-skipped kinds (nested functions, GNU VLS, DFP, va_arg_pack,
# preprocessor) live in SKIPPED.txt.
#

# === Crashes / Segfaults (2) ===
20011109-1.c
pr53748.c

# === Timeouts (2) ===
20151204.c
pr65680.c

# === Compile Rejects (1) ===
# labels-3.c uses GCC's label-as-values extension (`&&label`, `goto *p`), which
# FakeCC does not support. The other three rejects (init-1.c, pr64067.c,
# pr87647.c) were file-scope compound literals; they now compile after FakeCC
# learned to materialize `&((T){...})` as an anonymous static global.
labels-3.c
