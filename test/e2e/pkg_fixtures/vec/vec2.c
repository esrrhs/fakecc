/* Second file of the vec package: exports add().  Both files fold into one
 * package, so a single import vec sees scale() and add() together. */
package vec;

int add(int a, int b) { return a + b; }
